# 算法结构

> 上位机算法的分层骨架、世界模型、决策层接口与离线开发闭环。
> 配套：[`repository_structure.md`](repository_structure.md)（仓库结构）

---

## 0. 总览

**6 层 + 1 装配根轴 + 5 横切面。**

| 记号 | 含义 |
|---|---|
| **L1–L5** | 纵向层，数字越大越靠近「智能与策略」，**依赖严格单向向下** |
| **R** | 装配根轴（不是层）——兵种差异的唯一落点 |
| **X1–X5** | 横切面（不是层）——贯穿所有层，以契约/能力形式提供 |

**三条全局纪律**
1. 依赖严格单向向下；装配根向上组合。**进 CI 校验**，否则文档写完就烂。
2. 横切面禁止被业务层 `include` 其实现，只允许使用其接口。
3. **R 是唯一允许「知道全局」的地方**，且不含任何算法逻辑。

---

## 1. 分层骨架

```
════════════════════════════════════════════════════════════════════════════
 R  装配根轴  ── 兵种差异的唯一落点，唯一允许「知道全局」的地方
    sentry_root · infantry_root
    职责：模块组合 · 参数注入 · 进程/执行器拓扑 · namespace · sim/real 切换
    纪律：不含任何算法逻辑
════════════════════════════════════════════════════════════════════════════
                                    ▲ 组合 L1–L5（依赖向下 / 装配向上）
════════════════════════════════════════════════════════════════════════════
 L5 决策与仲裁                                    1Hz ──► 50Hz ──► 指令
    ├ L5-a 战术层    RL ∥ 规则(打分函数/状态机)   ← 同一接口，可互换
    ├ L5-b 技能层    状态机/行为树 展开 + 前置条件检查 + 安全屏蔽
    └ L5-c 安全门    指令仲裁(底盘/云台/发射权限) · 开火授权 · IFF
════════════════════════════════════════════════════════════════════════════
                                    ▲ WorldSnapshot
════════════════════════════════════════════════════════════════════════════
 L4 功能应用      自瞄(多源目标注入)          │   导航(静态地图规划+动态避障)
                  跟踪与伺服 100–1000 Hz
════════════════════════════════════════════════════════════════════════════
                                    ▲ 目标 / 动态障碍
════════════════════════════════════════════════════════════════════════════
 L3 世界模型      定位服务 ★安全关键 · 地图管理 ★契约资产 · 动态物体检测
                  · 态势融合 · 目标感知 · 状态估计 · 赛场态势
    → WorldSnapshot  (内置地图    (2D+3D同源  (配准残差)    (多源仲裁)  (装甲板)
      纯函数           +激光配准)   ·版本化)
                  ← 环境感知(地形/代价) 整层删除
════════════════════════════════════════════════════════════════════════════
                                    ▲ 位姿 / 点云 / 图像 / 报文
════════════════════════════════════════════════════════════════════════════
 L2 I/O 抽象      下行 HAL          │  上行中间件端口        │  横向协同端口
    传感器(激光/IMU/相机)             pub/sub/srv/action      机间通信
    执行器(底盘/云台/发射)            抽象 · QoS 档位          能力声明 · 版本协商
    总线(串口/CAN/USB)               执行器/回调组策略        带宽预算 · 降级
    设备生命周期(open/close/recover)                         异步 best-effort 非阻塞
════════════════════════════════════════════════════════════════════════════
                                    ▲ 设备 / 传输 / 网络
════════════════════════════════════════════════════════════════════════════
 L1 平台基础      时间与时钟 · 数学/几何/滤波(EKF/PID) · 无锁队列/线程池
                  参数系统(类型化 schema + 校验 + 热加载) · 错误码与结果类型
                  日志/录制/回放基础设施 · 状态编码器纯函数(训练与部署共用)
════════════════════════════════════════════════════════════════════════════

╔══════════════════════════════════════════════════════════════════════════╗
║ 横切面 ── 贯穿 L1–L5，以「契约/能力」形式提供                              ║
║                                                                          ║
║  X1 健康与安全   心跳/存活 · 急停硬路径 · 定位健康 · 开火安全门(IFF/授权)  ║
║  X2 接口契约     机内接口 · 机间协议 · 地图版本 · HMI 呈现消息             ║
║  X3 时间与同步   机内同步 · 跨机器人时钟对齐 · 新鲜度/TTL                  ║
║  X4 可观测调参   日志 · 录制回放 · 指标 · RL决策记录(state/action/mask)    ║
║  X5 分布式协同   带宽预算 · 能力/版本协商 · 队友上下线降级 · 态势一致性     ║
╚══════════════════════════════════════════════════════════════════════════╝
```

> **下位机（MCU）不在这套分层内。** L1–L5 与装配根描述的全部是**上位机**；
> MCU 固件位于 L2 下行 HAL **之下**，属于硬件侧（契约见 §6.1，实现见 `ControllerCode`）。
>
> X2 的「机内接口」包含**两类不同的东西**，不要混为一谈：
> - **ROS 层**——`msg` / `srv` / `action`，用于机内进程间通信
> - **线协议层**——上位机 ↔ 下位机的 USB CDC 帧格式，用于跨越上位机/下位机边界

### 1.1 层职责

| 层 | 一句话职责 | 关键纪律 |
|---|---|---|
| **L5** | 决定「做什么」 | RL 只能输出**意图**，安全包络由下层强制 |
| **L4** | 实现「怎么做到」 | 每个功能 = 独立 package，可独立启停/测试/回放 |
| **L3** | 回答「世界是什么样」 | 输出**唯一**的 WorldSnapshot；定位是安全关键 |
| **L2** | 屏蔽「东西怎么连」 | 全部以**接口/端口**暴露，实现可替换 |
| **L1** | 提供「谁都能用的基础」 | 零业务语义，被所有人依赖，最稳定 |
| **R** | 决定「这一台是什么车」 | 唯一允许知道全局；不含算法逻辑 |

**决策分两处**：全局战术在 L5；局部决策（`aimer`/`shooter`、导航 recovery）内嵌 L4 各功能。

### 1.2 R 装配根轴

| 项 | 内容 |
|---|---|
| **实例** | `sentry_root` · `infantry_root`；同兵种多实例 = 同根 + 不同 namespace + 独立参数 |
| **职责** | 选择并实例化模块组合 · 注入配置 · 决定进程/线程/执行器拓扑 · 设置 namespace/remap/TF 静态外参 · 切换 sim / real / 无硬件回放 |
| **禁止** | 包含任何算法逻辑；生产根绝不链接调试逻辑 |
| **两套入口** | 生产装配根与调试装配根**都进仓库、都进 CI**；禁止 `#ifdef DEBUG` 式分支 |

**兵种差异只有三个来源**
1. 硬件拓扑 —— L2 HAL 抽象接口 + 可替换驱动
2. 传感器配置 —— L2 驱动插件 + 参数系统
3. 策略与参数 —— L5 行为树/策略数据 + 参数分层覆盖

**参数两级覆盖**

```
config/shared.yaml          ← 全兵种共享（坐标系统一、通用阈值）
   ↑ 覆盖
config/<robot_type>.yaml    ← 兵种 + 场景差异（sentry / standard3；sim / reality）
```

**复用度量（可测指标）**

| 指标 | 目标 |
|---|---|
| 新增兵种改动行数 / 总行数 | < 10% |
| 跨层反向依赖数 | 0 |
| 策略改动的重编译次数 | 0 |

### 1.3 L5 决策与仲裁层

三子层，**分层依据是时间尺度分离**：

| 子层 | 频率 | 职责 | 技术 |
|---|---|---|---|
| **L5-a 战术层** | 1–5 Hz | 选技能 / 设定子目标 | RL 推理 或 可解释打分函数 / 状态机 |
| **L5-b 技能层** | 10–50 Hz | 把技能展开为可执行行为序列；强制安全包络与前置条件；**产出 ActionMask** | 状态机 / 行为树 |
| **L5-c 安全门** | 事件驱动 | 仲裁「谁有权发指令」；正常指令 ↔ 降级指令切换 | 状态机 `NONE/OPERATING/SUCCEEDED/FAILED`；RTI / MRM / MRC |

- **技能形态（3v3 哨兵）**：巡逻 / 追击 / 生存 / 回血，约 **4–5 态**的最小状态机。
- **策略数据化**：行为树 XML + 参数 YAML，换赛季/换兵种**不重编译**。
- **冻结的产物接口**（L5-a → L5-b → L4）：技能 ID 枚举 + 参数格式 + 前置条件定义 + 安全屏蔽（action masking）规则。
- **RL 上线顺序**：规则基线 → 规则+RL 离线评估 → **RL 影子模式（跑但不控）** → RL 接管（一键切回）。
- **状态编码器必须是纯函数**，放 L3；训练注入与部署同等的观测噪声与随机缺失（observation dropout）；状态必须全部可观测。

### 1.4 L4 功能应用层

**自瞄 `auto_aim`** —— 目标源为可插拔**多源融合**；只消费融合后的目标，不关心来源：

| 来源 | 特性 |
|---|---|
| 自身相机（装甲板识别） | 高精度、低延迟、视野受限 |
| 哨兵共享态势 | 覆盖广、精度低、有延迟 |
| 队友共享 | 同上，来源可能重复 |
| 裁判系统 | 无位置，仅血量/状态 |

- 目标源必须**过滤**友军（不只是标记友军）。
- 跟踪环仍为**硬实时**（不因人在环而放宽）。

**导航** —— 基于机内预置地图规划 + 动态障碍规避；**不用在线 SLAM**；激光不参与避障/地形/代价地图。

每功能自带局部决策，独立 package、独立启停/测试/回放。

### 1.5 L3 世界模型

对外输出**唯一**世界模型 `WorldSnapshot`（区域级抽象状态，非原始传感器数据），字段带 `stamp` + 置信度 + 新鲜度。

**定位服务**：内置先验地图 + 激光配准。因为激光只做定位 ⇒ 构成**单点故障**，故为**安全关键服务**。

> **健康契约**：位姿 + 置信度/协方差 + 新鲜度 + 失效标志。

**地图管理**：2D 栅格图与 3D 先验点云**必须同源、同坐标系、同原点**；版本化并随赛季规则版本绑定；纳入 X2 管理。

> **场地坐标系原点全队统一**，对齐规则定义的场地原点（不是机器人启动位置）——这是机间共享坐标的前提。

**动态物体检测（配准残差流水线）**

```
实时点云 ──配准──► 内置先验地图
                    ├──► 位姿 T          = 定位
                    └──► 配准残差 {p}    = 动态物体集合
                                            （地图里没有的点 = 场上会动的东西）
```

- 残差为**无标号**动态物体集合（敌方 + 己方队友 + 场地可动元素）。
- **区分敌我**：队友广播自身位姿 − 己方 = 敌方。
- 需区分「地图过期」与「动态物体」：残差聚类 + 持续时间判定 + 先验「允许动态区域」标注。

**状态估计链**：`point_lio` → `loam_interface` → `sensor_scan_generation`（保留，定位刚需）。

**已删除**：`terrain_analysis` / `terrain_analysis_ext` / `IntensityVoxelLayer` / `pointcloud_to_laserscan` / 在线 SLAM。

**保留的第三方刚需**：`small_gicp_relocalization`（升级为定位手段）、`fake_vel_transform`（云台自旋伪坐标系，哨兵必需）、NAV2 规划控制 + `pb_omni_pid_pursuit_controller`、`ign_sim_pointcloud_tool`（仿真喂点云给 LIO）。

### 1.6 L2 I/O 抽象层

全部以「接口/端口」形式暴露，实现可替换。**不直接到处使用 ROS 2**（否则无法单测、无法去 ROS 化、无法换中间件）。

**三条腿**
1. **下行 HAL** —— 设备与执行器
2. **上行中间件端口** —— pub/sub/srv/action 抽象
3. **横向协同端口** —— 机间通信，独立于具体 DDS 实现

**设备生命周期与自恢复**：`open` / `close` / `recover`，`ERROR` 态尽量自修复。

**HAL 依赖倒置**：抽象接口在 L2，具体驱动实现可放 L2 之下的插件目录，依赖方向恒为「实现依赖抽象」。

### 1.7 L1 平台基础层

零业务语义，被所有层依赖，最稳定：时间与时钟 · 数学/几何/滤波(EKF/PID) · 无锁队列/线程池 · 参数系统 · 错误码与结果类型 · 日志/录制/回放基础设施 · 状态编码器纯函数。

> 接口稳定；生产构建可关闭可观测实现，但接口不变。

---

## 2. 横切面

### 2.1 X1 健康与安全监管

原「看门狗」**不设层**，拆为四件：

| 子能力 | 回答的问题 | 时间尺度 | 机制 |
|---|---|---|---|
| **a) 心跳/存活** | 进程还活着吗？数据还在流吗？ | ms–100 ms | 消息带 `timestamp + sequence`；QoS `deadline` 检测停流、`liveliness` 检测发布者失活 |
| **b) 状态/故障诊断** | 哪个功能单元坏了？坏到什么程度？ | 100 ms–1 s | 功能单元故障树(DAG)；error level 同 `diagnostic_msgs/DiagnosticStatus`；静态结构发一次 + 动态状态周期发；级别可 latch + reset |
| **c) 失效安全/降级仲裁** | 现在谁有权发指令？失效时退到什么行为？ | 事件驱动 | 一道 Gate，在「正常指令」与「降级指令(最小风险行为)」间切换 |
| **d) 任务生命周期管理** | 这个功能该跑还是该停？ | 事件驱动 | 决策节点以 service 管理功能节点：`GetTaskStatus` / `ControlTask` |

**三条铁律**
1. **自报 + 订阅**：模块只以统一接口暴露自身健康，**绝不 include 监管者**（禁止反向依赖）。
2. **急停必须有硬路径**：最终安全降级（`cmd_vel` 超时归零、发射机构断电）**不依赖中间件**；放最靠近执行器的 L2 执行器抽象内部，独立实时线程 + 超时定时器。
3. **b 与 c 分离**：诊断只报告状态，仲裁只决定权限。

**当前裁剪（3v3 起步）**：诊断与任务生命周期后置；保留心跳/存活 + 急停硬路径；**定位健康必须进 X1**；**开火安全门（IFF / 授权）进 X1**。

### 2.2 X2 接口契约

- **接口包独立于实现包**（独立成仓）。
- 语义版本化；**外部协议版本钉死**（裁判系统串口协议 `V1.7.0 (20241225)`，`referee/` 下 7 个消息）。
- **消息设计规范**：单位与坐标系遵循 REP-103 / REP-105；必须带 `stamp` 与 `frame_id`；估计类消息带置信度。
- **云台角约定全队统一**：yaw 向左为正、pitch 向下为正（**与 DJI NED 约定不同，必须在契约中写死**）。
- **变更流程**：评审 + 影响面清单 + 兼容期。
- 地图版本、机间协议、HMI 呈现消息定义均纳入 X2。

### 2.3 X3 时间与同步

- 统一时钟源；`use_sim_time` 在 sim/real 切换时所有节点行为一致。
- **时间戳契约**：谁打、何时打（采集时刻 / 接收时刻）、跨域传递是否重打。
- **数据新鲜度**：每个输入定义「多久算过期」，过期即降级（这是 X1 的输入前提）。
- TF 树完整性与时间插值容差的**启动自检**。
- 范围扩张：从机内同步扩到**跨机器人时钟对齐**（机间态势的前提）。

### 2.4 X4 可观测与调参

- **观测点标准化**：统一命名、统一单位、统一下采样策略；高频数据必须可降采样后观测。
- **可复现优先**：录制/回放 > 实时可视化；目标为「赛后能把问题重现」。
- **调参不影响实时**：参数热加载走非实时域；危险参数需鉴权与审计。
- **可视化外置**：rviz2 / Foxglove / PlotJuggler，不写进业务模块。
- RL 模块记录 `(state, action, mask, value/prob)`。
- HMI 呈现的**实现**归 X4，**消息定义**归 X2。

### 2.5 X5 分布式协同

带宽预算 · 能力声明与版本协商 · 队友上下线降级 · 态势一致性模型。

---

## 3. 世界模型与态势闭环

### 3.1 形成过程

```
 激光 ──► 配准 ──► 内置先验地图(2D栅格 + 3D点云, 同源同坐标系)
            ├──► 位姿 + 置信度 ──────────────────────┐
            └──► 配准残差 ──► 动态物体聚类 ─────┐     │
                                                │     │
 相机 ──► 装甲板识别/跟踪 ──► 敌方(自视) ───────┤     │
                                                ▼     ▼
 队友广播自身位姿 ─────────────► ┌───────────────────────────┐
                                 │  态势融合                  │
 裁判系统 ──► 己方状态/比赛阶段 ►│  去重 · 坐标统一 ·         │
                                 │  时间对齐 · 置信度仲裁      │
                                 └─────────────┬─────────────┘
                                               ▼
                                ┌──────────────────────────────┐
                                │ 世界模型 → WorldSnapshot      │
                                │ (纯函数 · 定长 · 区域离散化 ·   │
                                │  带 age/confidence · 仿真可复现)│
                                └──┬────────────┬────────────┬─┘
                                   │            │            │
                   ┌───────────────┘            │            └──────────────┐
                   ▼                            ▼                           ▼
       动态障碍 → costmap 层          态势向量 → L5 决策           目标 → 自瞄 (L4)
       (导航 L4 使用)                 (RL/规则 同构输入)           (多源目标注入)
```

### 3.2 态势闭环

```
内置地图 ⇒ 配准残差 = 「无标号的动态物体」（含己方 + 敌方 + 弹丸）
⇒ 队友广播自身位置，减去己方 ⇒ 剩余即敌方
⇒ 敌方态势广播给步兵 ⇒ 步兵半自动攻击（操作手开火授权）
```

**哨兵天然是队伍的「态势节点」**：它有地图，所以能算出「什么不该在那儿」。

```
哨兵: 激光 ─配准→ 位姿 + 残差(动态物体)
                            │
队友: 各自广播自身位姿 ───────┤ 减去己方
                            ▼
                       敌方态势 (带时间戳 + 置信度)
                            │
                   ┌────────┴────────┐
                   ▼                 ▼
             哨兵自身决策        步兵半自动攻击
             (态势向量)         (外部目标注入)
```

### 3.3 `WorldSnapshot` 设计原则

1. **定长**：不用变长数组（RL 需固定输入）；缺失用 `valid` 标志。
2. **区域离散化优先**：用区域 ID 代替原始坐标 → 降维 + **大幅缩小 sim2real gap**。
   > 这是**关键耦合**：区域化状态设计 → 使轻量对局仿真成为可能 → 使红队 RL 可行。
   > 若状态用原始连续坐标，轻量仿真立刻变得困难。
3. **带 `age` 与 `confidence`**：让决策器知道信息**多旧**、**多可信**。
4. **纯函数、零 ROS 依赖**：可单元测试、可被仿真与实车共用。

### 3.4 `WorldSnapshot` 字段分区

**A. `self`（自身）**

| 字段 | 说明 |
|---|---|
| `region_id` | **所在区域 ID（离散化）** ← 关键 |
| `pos_in_region` | 区域内归一化位置（可选，0–1） |
| `yaw`, `v` | 朝向、速度（归一化） |
| `hp`, `hp_max` | 血量 |
| `ammo` | 剩余弹丸 |
| `alive` | 是否存活 |
| `respawn_timer` | 复活倒计时 |
| `buff_state` | 增益状态位域 |

**B. `allies[2]`（队友，固定 2 台）**

| 字段 | 说明 |
|---|---|
| `valid` | 信息是否有效（通讯可能丢失） |
| `age` | **信息年龄（秒）** ← 决策器需知道信息多旧 |
| `region_id`, `hp`, `alive` | 状态 |
| `robot_class` | 步兵 / 英雄 |

**C. `enemies[3]`（敌方，固定 3 槽，按置信度降序）**

| 字段 | 说明 |
|---|---|
| `valid` | 该槽是否有目标 |
| `region_id` / `pos` | 位置 |
| `hp_estimate` | 估计血量（未知时 `valid_hp=false`） |
| `confidence` | **置信度** |
| `source` | 来源枚举：`OWN_VISION` / `SENTRY_SHARED` / `FUSED` |
| `age` | 信息年龄 |

**D. `game`（比赛）**
`phase`（准备/战斗/结算）· `time_left` · `own_base_hp` · `enemy_base_hp` · `score`

**E. `context`（派生）**
到各关键区域的转移时间（查预计算距离矩阵）· 人数优势 · 威胁度

**F. `temporal`（时序）**
`last_skill_id` · `last_skill_duration` · `time_since_last_damage` · `time_since_last_fire`

---

## 4. 决策层接口

### 4.1 统一签名

```cpp
Decision decide(const WorldSnapshot & world, const ActionMask & mask);
```

**三件套**：`WorldSnapshot`（态势输入）· `ActionMask`（可行性/前置条件）· `Decision`（产物）

### 4.2 `mask` 必须进接口

因为 `(state, mask) → action` 正是 **masked policy** 的标准形式。把 mask 放进接口，让四种实现**结构上完全同构**：

| 实现 | 如何映射 |
|---|---|
| 有限状态机 | 内部状态 + snapshot → skill |
| 行为树 | 树的行为节点输入 = snapshot + mask |
| 效用打分 | 每个 skill 算分 → 在 mask 内 argmax |
| **RL 策略** | snapshot(定长向量) → 网络 → logits → **mask → 采样/argmax** |

**收益**
- 对 RL：mask 逻辑训练与部署**共用同一份代码** → 直接消除「训练-部署不一致」这一最大的 RL 工程坑
- 对规则：mask 即「前置条件检查」，避免选出不可行技能
- **两者共用同一个 mask 计算函数（由技能层 L5-b 提供）**，不重复实现

```
            WorldSnapshot（定长 · 区域离散化 · 带 age/confidence）
                              │
            ActionMask（由 L5-b 技能层计算的前置条件可行性）
                              │
                              ▼
              ┌─────────────────────────────────┐
              │   decide(world, mask)           │  ← 唯一接口
              │   (state, mask) → action        │
              └────────────────┬────────────────┘
                               │
      ┌──────────────┬─────────┴──────────┬──────────────────┐
      ▼              ▼                    ▼                  ▼
  有限状态机     行为树           效用打分(推荐起点)      RL(masked policy)
  内部状态+快照  节点端口=快照    每技能算分→mask内argmax  网络→logits→mask→采样
      │              │                    │                  │
      └──────────────┴─────────┬──────────┴──────────────────┘
                               ▼
                Decision { skill_id, params, confidence, rationale }
                               │
                               ▼
       L5-b 技能层展开 → L5-c 安全门校验 → L4 执行(自瞄/导航)
```

### 4.3 数据结构

```
ActionMask
  feasible[SKILL_COUNT]      : bool
  reject_reason[SKILL_COUNT] : enum（调试用）

Decision
  skill_id   : enum
  params     : 按 skill 定义（如 target_region_id / target_enemy_index）
  confidence : float（可选）
  rationale  : 调试字段（规则=打分明细 / RL=action_prob+value）
```

`ActionMask` 由**技能层**计算（前置条件：血量 / 弹药 / 位置 / 比赛阶段 / 目标存在性）。

### 4.4 安全约束

**RL 输出的是「意图」，安全包络由 L5-b / L5-c 强制，不可由 RL 自觉。**
训练时也必须施加 mask 与观测噪声。

### 4.5 RL 动作空间

| 阶段 | 动作空间 | 评价 |
|---|---|---|
| 推荐起点 | 离散技能选择（巡逻/追击/撤退/防守/回血/压制） | 最小可行 |
| 第二阶段 | 技能 ID + 离散目标区域 | 值得做 |
| 第三阶段 | 连续子目标位姿 | 较难 |
| **禁止** | 直接输出速度 / 云台角 | 安全不可控 |

---

## 5. 安全语义

### 5.1 步兵开火必须人工授权

不存在自动开火路径；**安全门不得由 RL 或视觉模块自行放宽**。安全要求因此转移为两条：

1. **禁止把友军作为瞄准目标** —— `auto_aim` 目标源必须**过滤**友军（是过滤，不是标记）
2. **呈现必须保真** —— 敌我身份与置信度必须清晰、不可混淆

### 5.2 HMI 呈现契约

| 需呈现 | 要求 |
|---|---|
| 目标位置 / 朝向 | 实时更新，带 TTL 可视化（**过期目标必须明显标出**） |
| 敌我身份 | 必须显著，不可与目标框混淆 |
| 置信度 | 低置信度与高置信度视觉可区分 |
| 信息来源 | 区分「自己相机看见」vs「哨兵共享」 |
| 授权状态 | 当前锁定目标是否已授权、授权是否仍有效 |

> 消息定义归 X2，实现归 X4。

### 5.3 授权作用域

授权绑定**具体目标身份**；目标丢失或切换 ⇒ **授权自动失效** ⇒ 需重新授权。
（语义类似「扳机保持」，非「一次授权」。）

### 5.4 IFF 保守取向

代价不对称 ⇒ **宁可漏报，不可误报**；低置信度目标不得以「确定敌方」方式呈现。

### 5.5 延迟预算

人在环允许**数百毫秒**级端到端延迟（决策与态势实时性压力下降）。
但信息时效必须可视化，**过期态势不得静默使用**；`auto_aim` 跟踪环仍为硬实时。

---

## 6. 关键契约

### 6.1 必须优先冻结的四份

| # | 契约 | 方向 | 为什么优先 |
|---|---|---|---|
| **0** | **上位机 ↔ 下位机链路协议** | L2 下行 HAL ↔ MCU | **阻塞最直接**（电控组拿不到帧格式就写不了固件）；**最稳定**（硬件定了基本不动）；跨两个组 |
| 1 | **世界模型接口** | L3 → L5/L4 | 所有层的共同输入；必须能被仿真复现（仿真输出 schema 与实车一致） |
| 2 | **决策产物接口** | L5-a → L5-b → L4 | 技能 ID 枚举 + 参数格式 + 前置条件定义 + 安全屏蔽规则；规则与 RL 可互换的前提 |
| 3 | **机间态势协议** | 哨兵 ↔ 步兵 | 跨兵种共享，涉及多个仓 |

> **先定接口，后定算法**——接口对了，打分函数与神经网络可互换。

#### 上位机 ↔ 下位机链路协议

| 项 | 内容 |
|---|---|
| 物理层 | **USB CDC**（`/dev/ttyACM*` → udev 固定设备名） |
| 帧格式 | `SOF(2) + LEN(1) + SEQ(1) + MSG_ID(1) + PAYLOAD(≤250) + CRC16(2)` · 全小端 · 单精度浮点 |
| 三层归属 | 规范 / 定义 / 生成物 → `foray_interfaces`（X2）· 上位机实现 → `foray_platform`（L2）· 下位机实现 → `ControllerCode` |
| 单一事实来源 | **两侧代码由生成器产出，禁止手写**——否则字节序 / 字段偏移 / CRC 必然漂移 |
| 硬路径 | **失联退化由下位机自主执行**，不依赖上位机（见 §5） |
| 现状 | 草案 v0.1 → `foray_interfaces/protocol/lower_link.md` |

> ⚠️ **宿主侧最易漏的一项**：Linux `cdc_acm` 的 `latency_timer` 默认 **16 ms**。
> 不改成 1，实时预算当场归零。
>
> ⚠️ **为什么 CRC 不能省**：USB 链路层自带 CRC16，但同一帧格式要复用到 CAN（电调）与
> UART（遥控器 SBUS）。保留 CRC 换取格式可移植性，成本每帧 2 B。
>
> ⚠️ **这条链路跨算法组与电控组**——规范放 `foray_platform`（实现仓）会让电控组
> 必须读算法组的仓，且协议改动会藏在平台重构 PR 里无人审查（违反 X2 的 C1/C6 判据）。

### 6.2 机间态势协议

| 项 | 内容 |
|---|---|
| 字段 | 位置 · 朝向 · 置信度 · `stamp` · TTL · 来源 |
| 一致性模型 | **最终一致** + 时间戳 + 置信度（**非强一致**） |
| 共享内容 | **态势语义**，不是原始点云。哨兵本地完成「残差 → 聚类 → 敌方位置/朝向/置信度」 |
| 版本与能力 | 版本协商 + 能力声明 |
| 降级 | 队友下线时的退化行为 |
| 位置 | **绝不在实时路径上**；本地感知优先级永远高于共享信息 |

**带宽分级（由省到费）**

```
敌方位置+置信度  ≪  占据栅格增量  ≪  降采样点云  ≪  原始点云
```

> 点云只在本地消费，对外广播的是它提炼出的态势。

### 6.3 其余接口契约

| 契约 | 内容 |
|---|---|
| **心跳** | 消息含 `timestamp + sequence`（检测延迟、乱序、丢包） |
| **诊断** | 故障树 DAG；error level 同 `diagnostic_msgs/DiagnosticStatus`；静态结构 + 动态周期；latch + reset |
| **安全门** | 状态机 `NONE/OPERATING/SUCCEEDED/FAILED`；RTI / MRM / MRC |
| **任务管理** | service `GetTaskStatus` / `ControlTask`；状态 `RUNNING/IDLE/ERROR`；命令 `START/STOP` |
| **定位健康** | 位姿 + 置信度/协方差 + 新鲜度 + 失效标志 |

### 6.4 门槛检验

**把规则基线放进仿真，看胜负率是否与实车相符。**
仿真不可信 ⇒ 红队不可信 ⇒ 一切 RL 结论不可信。

---

## 7. 接口效率

### 7.1 实时域划分

| 域 | 时间尺度 | 内容 | 做法 |
|---|---|---|---|
| **硬实时**（**下位机本地**） | 1–10 ms | 电机电流环 / 位置环、发射机构闭环 | 跑在 MCU 上，本地闭环，**不经过 USB** |
| **硬实时**（上位机进程内） | 1–10 ms | `auto_aim` 跟踪环 → 生成目标角 | 去中间件，直连 HAL，独立实时线程，**禁止动态内存分配与锁** |
| **软实时** | 10–100 ms | 自瞄检测/跟踪、导航控制 | 进程内组合 + 零拷贝 |
| **非实时** | 100 ms–1 s | 决策、态势融合、日志、诊断、可视化 | 可容忍中间件开销 |

> ⚠️ **跨 USB CDC 的路径不属于硬实时域。**
> 上位机送的是**目标**（100–200 Hz），不是每周期指令；电流环与位置环在下位机本地闭环。
> 上位机侧 1–10 ms 的要求只对**进程内**算法路径（图像 → 检测 → 角度）成立。
> 契约见 §6.1。

### 7.2 进程内组合 + 零拷贝

高频链路（图像 → 检测 → 跟踪）放同一 component container；用 `RCLCPP_COMPONENTS_REGISTER_NODE` 注册；`std::unique_ptr` 发布取得进程内零拷贝。

### 7.3 QoS 命名档位

| 档位 | 设置 | 用于 |
|---|---|---|
| `sensor_data` | best_effort + 小队列 | 图像、点云 |
| `control_command` | reliable + `deadline` 监控 | `cmd_vel`、云台指令 |
| `latched_state` | `transient_local` | 地图、机器人描述、静态外参、比赛阶段 |

> 档位定义与选型理由落在 X2 契约文档。

### 7.4 执行器与回调组

哪些回调必须互斥 / 可并行 / 必须高优先级 = **装配根显式配置**，不依赖默认 `spin`。

### 7.5 其余

- **关键路径保留「去 ROS 化」能力**（算法层零中间件依赖）。
- **跨模块只传语义，不传原始数据**。

---

## 8. 离线开发闭环

### 8.1 关键洞察：决策层仿真不需要物理引擎

决策层消费的是 `WorldSnapshot`（区域级抽象状态），不是原始传感器数据。因此：

> 对局仿真 = **一个随机博弈（stochastic game）模型**，不是物理仿真。

| 需要建模 | 不需要建模 |
|---|---|
| 场地拓扑（区域图 + 转移时间矩阵） | 物理引擎 |
| 机器人（位置/HP/弹药/冷却/速度） | 渲染 |
| 命中概率模型 + 伤害 + 飞行时间 | 真实弹道积分 |
| 裁判规则（回血/复活/阶段/胜负） | 真实点云 / 图像 |
| 观测模型（可见性 + 噪声 + 延迟） | 传感器仿真 |

把「Gazebo 里跑 3v3」换成「一个带随机命中率的抽象对局模型」，工作量从「一个学期」降到「几周」。

### 8.2 轻量对局仿真最小建模集

| # | 建模项 | 说明 |
|---|---|---|
| 1 | **场地拓扑** | 区域图 + 区域间**转移时间矩阵**（预计算） |
| 2 | **机器人** | 区域位置、HP、弹药、冷却、移动速度 |
| 3 | **射击** | 命中概率模型 `p(距离, 目标状态)`、伤害、飞行时间、弹药消耗 |
| 4 | **裁判规则** | 回血、复活、比赛阶段、胜负判定 |
| 5 | **观测模型** | 可见性（视距/视野角/遮挡）+ 噪声 + 延迟 |
| 6 | **延迟模型** | 决策周期、通信延迟、执行延迟 |

**硬性要求**
- **输出 `WorldSnapshot` 的 schema 必须与实车完全一致**（这就是「仿真可复现」的具体含义）
- 无渲染、可并行、相同随机种子可复现
- **单局耗时 << 1 s**（否则千万级训练不可行）

### 8.3 红队 RL

**RL 当红队（对手生成器），不当控制器。** 红队永远待在仿真里 ⇒ 不需要 sim2real ⇒ 风险为零。

```
1. 规则决策器 = 我方 3 台的 policy
2. 红队 RL 控制敌方 3 台：状态 = WorldSnapshot，动作 = masked skill，奖励 = 胜负 (±1)
3. 训练红队至收敛
4. 评估：红队 vs 规则，跑 N 万局 → 统计胜率 + 收集「规则落败的状态」
5. 人审 triage
6. 修规则 → 回到 3（红队需重新训练，因为它已过时）
```

**第 6 步即对抗性课程循环**：规则变强 → 红队重新适应 → 发现新弱点。

**对手池（防过拟合）**：保留 **3–5 个历史红队检查点**，规则必须同时对抗整个池子。否则规则会过拟合到「最新红队」，而不是变强。

**triage：红队的发现是候选，不是结论**

| 类别 | 处置 |
|---|---|
| 真实弱点 | → 进规则待办，修补 |
| 仿真 artifact | → 记录，可能需要修仿真参数（不可能在真实中发生） |
| 规则违规策略 | → 收紧红队动作空间（红队钻了规则漏洞） |

**控制点**：红队必须被限制在**与真实对手相同的动作空间与能力范围**内。否则它找到的是「规则漏洞」而非「真实弱点」。

**示例产出**：`在己方双步兵阵亡、哨兵残血且比赛剩余 <60s 时，规则策略胜率显著低于 RL 对手`

**做 RL 的前提条件**
- 规则已到瓶颈，且失败模式可量化
- 仿真可信度检验通过
- ≥1 人长期负责
- 具备并行仿真吞吐（数十环境并行）

**更便宜的替代**（先试这些）
- 效用打分（取最高分，结构上等价于 policy）
- 打分函数 + 离线参数优化（CMA-ES / 贝叶斯优化）
- 行为克隆 / 模仿学习

**红线**：红队是**支线**，赛前没做完不影响交付；输出**只用于改规则**，不直接上实车，控制器上线不做。

### 8.4 实战数据采集（赛前就必须做好）

> **格式必须现在定。赛后补不了——字段没记，数据就永久作废。**

| # | 层 | 内容 |
|---|---|---|
| 1 | **决策层** | `(world_snapshot, mask, chosen_action, confidence)` 每决策周期 |
| 2 | **感知层** | 定位位姿 + 置信度；敌方检测（位置/置信度/来源）；残差检测结果 |
| 3 | **执行层** | 底盘指令、云台指令与实际角度、射击事件 |
| 4 | **裁判系统** | 原始报文（血量/弹药/阶段/胜负） |
| 5 | **元数据** | 统一时间戳、比赛 ID、**地图版本**、**代码版本(git hash)**、**参数版本** |

**对齐要求**：统一时钟（对齐 X3）· 单场比赛 = 一个 episode，可完整重放。

**影子模式记录**：`(state, RL action, 规则 action, mask, value)`

### 8.5 赛后闭环

**真实数据最高价值的用途不是直接训练 RL。**

- 直接训 RL = offline RL，需要**大量**数据，且有分布偏移（extrapolation error）
- 一个赛季可能只有几十场比赛的数据，**量级远远不够**

**应该做 system identification（系统辨识）**：辨识命中率分布、移动速度、决策延迟、通信延迟、观测噪声的**真实参数**，灌进仿真的射击/观测/延迟模型。

| 优先级 | 用途 | ROI |
|---|---|---|
| **1** | **校准仿真**（拟合仿真参数） | **最高** |
| **2** | **规则修补**（用红队失败清单改规则） | 直接见效 |
| 3 | 离线策略评估（offline RL / 反事实评估） | 数据足够后再考虑 |
| 4 | 在线 RL | 远期，需巨大投入 |

> 仿真越可信 → 红队越有效 → 规则修补越准；**而且主线的导航/感知测试同样受益**。

### 8.6 序时计划

| 阶段 | 工期 | 产出 | 验收标准 |
|---|---|---|---|
| **P0 接口冻结** | ~1 周 | `WorldSnapshot` / `ActionMask` / `Decision` schema + **区域划分表** | 规则决策器与 RL 能用**同一签名**实现（先写两个 mock 验证） |
| **P1 轻量对局仿真** | ~2 周 | 无渲染 3v3 对局仿真 | 单局 << 1s；同种子可复现；state schema 与 P0 一致 |
| **P2 规则接入** | ~1 周 | 规则决策器实现统一接口 | **同一份代码**在仿真与实车行为一致 |
| **P3 红队 RL** | 并行 2–4 周 | 红队策略 + 人审后的规则失败清单 | 红队胜率显著高于随机；≥N 条被确认为真实弱点 |
| **P4 数据采集** | 赛前 | 统一记录格式 + 回放工具 | 一场训练赛后可完整重放决策过程 |
| **P5 赛后闭环** | 赛后 | 校准后的仿真参数 + 修补后的规则 | 规则对红队胜率提升；仿真与实车胜率差异缩小 |

**每一块产出对主线都有独立价值**（即使永远不做 RL）

| 产出 | 不做 RL 时的用途 |
|---|---|
| 接口规范化（P0） | 规则决策、单元测试、模块解耦、回归测试 |
| 轻量对局仿真（P1） | 规则调参、战术演练、参数寻优 |
| 数据采集（P4） | 复盘、问题重现、规则修补、故障定位 |
| 仿真校准（P5） | 主线导航/感知测试的保真度 |

**人力**：1 人负责接口规范 + 轻量对局仿真（主线必需，非 RL 专属）；1 人负责红队 RL（支线）；主线组不受影响。

---

## 9. 演进阶段

| 阶段 | 目标 | 验收信号 |
|---|---|---|
| **P0 定契约** | 冻结 L2/L3 接口 + 建接口包 + 定 QoS 档位与命名规范 | 接口包可单独构建；每层归属 CI 可校验 |
| **P1 立骨架** | 建 L1 + L2 HAL 抽象接口 + 一个真实驱动实现 | 无算法情况下跑通「一个传感器 → 一个话题」 |
| **P2 打通单功能** | 最短闭环（相机 → 装甲板 → Target）打穿 L2→L3→L4 | 链路可独立启停、独立测试、可回放复现 |
| **P3 装配根** | 建 R：生产根 + 调试根 + 无硬件回放根 | 同一套 L1–L4 代码，三个根启动出三种系统 |
| **P4 决策与监管** | 行为树/状态机决策 + X1 四件套 | 能通过 service 启停功能；故障触发降级且可审计 |
| **P5 多兵种复用** | 第二兵种仅用「新 YAML + 新装配根 + 少量新驱动」接入 | 新增兵种改动行数占比 < 10% |
| **P6 效率收口** | 实时域划分、进程内组合、零拷贝、QoS 档位落地 | 高频链路无跨进程序列化；关键环延迟达标且可测 |

**起步落地范围**：L1 / L2 / L4 / R；L3 先薄；L5 最小状态机；X1 两件。
**单败淘汰下**：X1 可靠性 > L4 功能精度；鲁棒性 > 精度。

---

## 10. 治理机制

1. **包依赖图与方向检查进 CI**：每个 package 声明所属层（如 `layer:=L3`）；CI 校验 `L(n)` 不得依赖 `L(>n)`；横切面不得被业务层 include 实现。
2. **模块独立可测为硬门槛**：新模块必须带独立可执行测试；**不能独立测的模块 = 没解耦**。
3. **策略数据化**：全局策略、兵种差异、场景参数放 XML/YAML；新增兵种行为 = 新 XML + 新 YAML + 薄装配根，不重编译。
4. **两套入口并存**：生产装配根与调试装配根都进仓库、都进 CI。

---

## 11. 数值参数汇总

| 项 | 值 |
|---|---|
| 决策子层频率 | L5-a 1–5 Hz · L5-b 10–50 Hz · L4 跟踪伺服 100–1000 Hz |
| 实时域时间尺度 | 硬实时 1–10 ms · 软实时 10–100 ms · 非实时 100 ms–1 s |
| X1 时间尺度 | 心跳 ms–100 ms · 诊断 100 ms–1 s · 安全门与任务管理事件驱动 |
| QoS 档位数量 | 2–3 |
| 复用度量阈值 | 新增兵种改动行数占比 < 10% · 跨层反向依赖数 0 · 策略改动重编译次数 0 |
| 对手池检查点 | 3–5 个 |
| 并行仿真吞吐 | 需数十环境并行 |
| 人在环端到端延迟容忍 | 数百毫秒 |
| 裁判系统协议 | `V1.7.0 (20241225)`，`referee/` 下 7 个消息 |
| 参数覆盖层级 | 2 级（`shared.yaml` + `<robot>.yaml`） |
| 哨兵最小状态机 | 约 4–5 态 |

---

## 12. 3v3 裁剪

面向 **RMUL 3v3 区域赛**，相对 7v7 的主要裁剪：

| 项 | 处理 |
|---|---|
| 环境感知（地形分析整链） | **删除**——定位基于机内内置地图，激光只用于定位 |
| 定位 | **升级为安全关键服务**（带置信度 / 新鲜度） |
| 在线 SLAM | 删除；`slam_toolbox` 仅作离线建图工具 |
| 英雄 / 工程 / 飞镖 / 雷达站 | 不建 |
| 兵种阵容 | 步兵 ×2 + 哨兵 ×1 |
| X1 范围 | 诊断与任务生命周期后置；保留心跳 + 急停硬路径，**加定位健康 + 开火安全门** |
